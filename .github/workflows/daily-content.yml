name: Daily Instagram Content

on:
  schedule:
    # One scheduler entry. The application applies the exact Iran-local windows.
    # 06:00-08:00 and 16:00-18:00 UTC cover the requested 10:00-12:00 and
    # 20:00-22:00 Asia/Tehran windows; the Python code performs the exact edge check.
    - cron: '*/15 6-8,16-18 * * *'
  workflow_dispatch:

# The publisher commits only runtime state to the dedicated state branch.
# No pull-request or workflow administration permissions are granted.
permissions:
  contents: write

# Share the state branch lock with the comment workflow to prevent concurrent
# state/cache commits from racing and losing updates.
concurrency:
  group: climavids-instagram-automation-state
  cancel-in-progress: false

env:
  RANDOM_POST_CHANCE: '70'

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      # Content generation/preparation is safe, but live publishing stays off.
      DRY_RUN: 'true'
      PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
    steps:
      - name: Checkout main
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: main
          fetch-depth: 0
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Run tests
        run: python -m unittest discover -s tests -v

      - name: Prepare one daily Feed post
        run: python -m src.content_publisher

      - name: Prepare one daily Story (optional feature)
        run: python -m src.story_publisher
