# Configuration

`settings.example.json` contains non-secret defaults only.

Live credentials must be created under GitHub repository **Settings → Secrets and variables → Actions**. Never place their values in this directory or in workflow YAML.
